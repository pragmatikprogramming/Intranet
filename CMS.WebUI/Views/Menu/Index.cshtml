@{
    ViewBag.Title = "Menus";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="header">
    <h2>Menu's</h2>
</div>
<script type="text/javascript">
    var myID = 0;

    function doWork(id)
    {
        myID = id;
    }

</script>
<div class="container-fluid">   
<div class="row-fluid">  
    <div class="row-fluid">            
        <div class="btn-toolbar">
            <a href="/Menu/AddMenu"><button class="btn btn-primary"><i class="icon-plus"></i> New Menu</button></a>
            <div class="btn-group">
            </div>
        </div>
        <div class="well">

    <table class="table">
      <thead>
        <tr>
          <th>Menu Name</th>
          <th style="width: 36px;"></th>
        </tr>
      </thead>
      <tbody>
          @foreach (var menu in Model)
          {
                <tr>
                    <td><a href="/Menu/MenuItems/@menu.Id">@menu.MenuName</a></td>
                    <td>
                       <a href="/Menu/EditMenu/@menu.Id"><i class="icon-pencil"></i></a>
                       <a href="#myModal" role="button" data-toggle="modal" id="1" onclick="doWork(@menu.Id);"><i class="icon-remove"></i></a>
                    </td>
                </tr>
          }
      </tbody>
    </table>


        </div>
        <div class="modal small hide fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">x</button>
                <h3 id="myModalLabel">Delete Confirmation</h3>
            </div>
            <div class="modal-body">
                <p class="error-text"><i class="icon-warning-sign modal-icon"></i>Are you sure you want to delete the Menu?</p>
            </div>

            <div class="modal-footer">
                <button class="btn" data-dismiss="modal" aria-hidden="true">Cancel</button>
                <button class="btn btn-danger" data-dismiss="modal" onclick="document.location.href ='/Menu/DeleteMenu/' + myID;">Delete</button>
            </div>
        </div>
    </div>
</div>
